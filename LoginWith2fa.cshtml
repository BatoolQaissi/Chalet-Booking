@page
@using BookingApp.Areas.Identity.Pages.Account
@model LoginWith2faModel
@{
    ViewData["Title"] = "المصادقة الثنائية";
}

<h1>@ViewData["Title"]</h1>
<hr />
<p>حسابك محمي باستخدام تطبيق المصادقة الثنائية. الرجاء إدخال رمز المصادقة أدناه.</p>

<div class="row">
    <div class="col-md-4">
        <form method="post" asp-route-returnUrl="@Model.ReturnUrl">
            <input asp-for="RememberMe" type="hidden" />
            <div asp-validation-summary="All" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="Input.TwoFactorCode">رمز المصادقة</label>
                <input asp-for="Input.TwoFactorCode" class="form-control" autocomplete="off" />
                <span asp-validation-for="Input.TwoFactorCode" class="text-danger"></span>
            </div>

            <div class="form-group">
                <div class="checkbox">
                    <label asp-for="Input.RememberMachine">
                        <input asp-for="Input.RememberMachine" />
                        تذكّر هذا الجهاز
                    </label>
                </div>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-primary">تسجيل الدخول</button>
            </div>
        </form>
    </div>
</div>

<p>
    لا يمكنك الوصول إلى جهاز المصادقة الخاص بك؟ يمكنك 
    <a id="recovery-code-login" asp-page="./LoginWithRecoveryCode" asp-route-returnUrl="@Model.ReturnUrl">تسجيل الدخول باستخدام رمز الاسترداد</a>.
</p>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
